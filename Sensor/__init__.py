from dotenv import load_dotenv   # we do this to read the .env file
load_dotenv()      # load_dotenv() actually makes the variables in the .env file as Global variables hence we can
                   # use these variables in any file